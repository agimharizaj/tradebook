# Tradebook — project memory

This file is read automatically by Claude Code and Cowork when this folder is opened. It is the single source of truth for how to work on this project. Keep it updated as the build progresses.

## Product

Tradebook is a self-hosted trading companion for Gimi plus a couple of invited friends (hobby scale, free tiers, shared API keys). Possible productisation in 6-12 months; paid services only then. RLS keeps each user's data isolated already. It brings strategy playbooks, a trade journal, and a risk/position-size engine into one dark, terminal-grade web app. Inspired by EdgeFlo but market-agnostic. Web-first, mobile-friendly, eventually a phone-installable PWA.

## How to work with me (Gimi)

- I am a no-code/low-code practitioner, not a developer. Give exact commands and explain decisions plainly.
- One confident recommendation, not a menu. If an idea is bad, say so in the first sentence.
- Concise and direct. Never use em dashes.
- Before anything risky or irreversible (deleting data, changing auth, editing production), state the downside and the undo path first.
- I am happy to run things in the terminal via Claude Code as well as in Cowork.

## Stack (do not change without a stated reason)

- Next.js (App Router, TypeScript)
- Tailwind CSS v4
- Supabase: Auth, Postgres, Storage. Sessions via `@supabase/ssr` (cookie-based, refreshed in middleware).
- Hosting: Vercel, auto-deploy on push to `main`.
- Repo: GitHub.

## Architecture rules

- App Router. Server Components by default; `"use client"` only when interactivity is needed.
- Supabase clients live in `src/lib/supabase/`: `client.ts` (browser), `server.ts` (server), `middleware.ts` (session refresh). Never expose the service-role key to the browser.
- Every table has Row Level Security scoped to `auth.uid()`. Any new table ships with its RLS policies in the same migration.
- All schema changes go in a numbered file under `supabase/migrations/`. Never describe schema only in chat; write the SQL.
- Protected pages live under `src/app/(app)/` (auth checked in its layout). Public: `/login`, `/signup`, `/auth/*`.
- Only `NEXT_PUBLIC_*` env vars reach the browser.

## Brand and visual direction

The approved look is the dark "terminal" concept in `design/tradebook-brand-concept.html`. The current app pages still use a plain light theme and must be migrated to this system.

- Fonts: Space Grotesk (display/headings), Inter (body/UI), JetBrains Mono (all numbers).
- Core colors (lifted blue-charcoal ladder, never pure black): sidebar/page `#161A23`, app content canvas `#1C212C`, cards `#222834`, inputs/hover `#2B3240`, borders `rgba(255,255,255,.08)` and `.15`. Text `#E7E9EF`, muted `#9AA0B0`, dim `#757C8E`. Elevation ascends: sidebar < content < card < input.
- Accents: violet `#6A58F0` (primary/brand, chosen so white button text passes WCAG AA at 4.9:1; `#AB9DFF` lighter for text accents), teal-green `#22D39A` (profit/positive), red `#FF6274` (loss/negative), gold `#F3C57C` (premium highlight). Dim text is `#939AAD` (AA on all surfaces); do not darken either without rechecking contrast.
- Rationale: leading trading UIs avoid pure black (halation, eye strain over long sessions) and use a deep blue-slate base with a clear elevation ladder, one accent, off-white text. See research in git history / brand concept.
- Feel: institutional trading terminal, flat surfaces, subtle glow only on primary actions, numbers always monospace. No busy gradients on content, no clutter.
- Icons: inline SVG only (24 viewBox, strokeWidth 1.8-2, round caps/joins, currentColor), matching the existing set. Never use unicode arrows (← → ↑ ↓), ASCII glyphs, or emoji as UI icons - owner's rule. Sole exception: native `<option>` elements can't render SVG, so a text/emoji prefix there is tolerated.

## Current status

- Phase 1 (auth): DONE. Email + password (Google SSO removed by design, commit 08b6445), protected shell, sign out. Failed auth links (`/login?error=auth`) surface a visible error on the login form.
- Phase 2 (strategy builder): DONE. `/strategy` with read-only view + Edit mode, drag-reorder, checkboxes tickable in view, entry-model screenshot upload, risk controls, delete confirm modal.
- Phase 3 (journal): DONE. `/journal` monthly calendar, per-day trade add/list/delete modal, monthly + weekly summaries.
- Phase 4 (risk calculator): DONE. `/risk` with 3 modes (segmented switcher) and live prices via `/api/fx` (Frankfurter/ECB for fiat, CoinGecko for BTC, gold-api.com for XAU, all keyless). Pure sizing math lives in `src/lib/risk.ts` (lot output floors to 0.01 steps). Changing pair clears entry/SL/lots and prefills entry with the live price.
- Dashboard: real stats pulled from trades, incl. profit factor, equity curve, max drawdown. Read errors surface as a banner.
- Undo: BlockEditor has Cmd+Z / Cmd+Shift+Z whole-list snapshot history (typing grouped ~1s, native textarea undo overridden). Note/strategy deletes soft-delete with a 5s "Deleted - Undo" toast; the DB row is removed only when the toast expires or on unmount (finalizePendingDelete). Backspace/Delete opens the confirm dialog (Enter confirms, Escape cancels) outside text fields.
- Dashboard is stats + charts only (nav link cards removed); checked strategy entry/exit criteria render struck through.
- Journal: true expectancy (win% x avg win - loss% x avg loss) plus separate Avg R; MT5 import keeps close time and stores commission+swap in `trades.commission` (pnl stays net).
- Journal (EdgeFlo parity, Aug 2026): fetches the whole 6-week grid (weeks crossing months are complete); $/%/R display toggle (localStorage `tb_journal_unit`); calendar day cells show one dot per trade (filled violet = journaled, hollow = not; >6 trades collapses to "n/m journaled"); Monthly summary adds an R-distribution histogram; Weekly breakdown rows are clickable. Clicking a day or week opens `JournalPanel` (right drawer, `src/components/journal/JournalPanel.tsx`, one component with day|week scope): net PnL + arrows + Today, win rate/avg R/trades/wins/losses/journaled, plan-followed Yes/Partial/No (day) or per-day dots (week), guardrail violations (computed live via `computeViolations` in `src/lib/settings.ts`, never stored), pre-market routine ticks, day note with dictation, trades list with All/Wins/Losses filter + instrument search + pair flags (`PairFlag.tsx`) + entry→exit emotions; rows open the trade page, pencil opens quick-edit. Old DayModal replaced by `TradeFormModal.tsx` (backdrop/Escape close with dirty confirm, optional UTC time field, Save & Journal jumps to the trade page); ImportTradesModal closes on backdrop/Escape only before a file is loaded.
- Trade journal: `/journal/trade/[id]` (`TradeReview.tsx`). Regular mode = facts card (PnL, session derived from time, R, commission, edit/delete) + charts + review; Focus mode (localStorage `tb_journal_focus`) hides facts; Previous/Next walks the same day. Review autosaves (700ms debounce) to `trade_reviews`: plan-followed checkbox, plan picker (snapshots `strategy_name` so deleted plans still resolve), confluence/management/mistake tags (`TagInput.tsx`, catalogs derived from past reviews), fixed EdgeFlo emotion sets (`ENTRY_EMOTIONS`/`EXIT_EMOTIONS` in settings.ts), reflection with dictation, HTF/MTF/LTF screenshots in entry-models under `<uid>/reviews/<tradeId>/`. "Journaled" = any review content (`isJournaled`).
- Voice: `src/lib/useSpeech.ts` (Web Speech API, auto-restarts through Chrome's silence timeout) + `MicButton.tsx`; used in trade reflection, day note, and the Notebook palette Dictate button (inserts at cursor via insertText).
- Settings: `/settings` (`SettingsWorkspace.tsx`, tabs deep-linkable via ?tab=). Trading = guardrails (user_settings: max trades/day, max daily loss/profit as figure-or-%, trading windows "HH:MM-HH:MM TZ" with UTC/London/NY/Tokyo/Sydney aliases, warn-on-charts toggle) + trading profile (still auth metadata: broker/currency/account size/risk/experience/style/markets - moved from Profile). Pre-market routine = editable/reorderable items + browser-notification reminder time. Pairs = PairsManager (moved; `/profile/pairs` redirects to `/settings?tab=pairs`). Appearance = ThemeToggle. Tutorial & help = replay tour. Sidebar footer + mobile nav gained the Settings entry; Profile keeps personal details + security only. All new tables degrade gracefully when their migration is missing (notice + no persistence).
- Charts trading-day panel (`TradingDayPanel.tsx`): docked RIGHT of the chart, outside `#tv-chart-area` so captures stay clean; collapsible (localStorage `tb_tradingday_collapsed`, slim rail shows a pulsing dot when violations exist); mobile gets a "Day" toolbar button opening it as a bottom sheet. Shows routine checkboxes (persist per-day in day_reviews, reset daily by design - not on close), trades-today dots vs max, trading-window open/closed pill (minute tick), net PnL bar between loss cap and target, violation warnings, today's trades list linking to their journal pages. Routine reminder fires one browser notification per day at `routine_remind_at` while the app is open (no service worker push).
- Share Trade: Share button on the trade page opens a dialog with a private link (the page URL) and a revocable public link. Public view: `/share/trade/[token]` (server component, service-role client, robots noindex, token-scoped read-only). Middleware `isPublic` covers `/`, `/share`, and the auth pages - the landing page was unreachable signed-out before this (every visitor got bounced to /login).
- Chart snapshot menu (`SnapshotMenu.tsx`): camera button on Charts opens Download image / Copy image / Copy chart link (deep link `?tvwidgetsymbol=`) / Open in new tab / Post on X (X can't take images by URL, so it copies the shot and opens the composer). Dropdown is position:fixed because the toolbar's overflow-x-auto clips absolute children. All captures go through `captureChartArea` (user-approved tab capture; TradingView iframe is cross-origin).
- Shared risk inputs: `src/components/RiskFields.tsx` exports `MoneyOrPct` (figure-or-% with live "= X of account now" hint) and `WindowPicker` (time pickers + IANA tz select) - used by both StrategyWorkspace risk controls and Settings guardrails. `src/components/CurrencySelect.tsx` is the account-currency dropdown (risk calculator, Settings trading profile, account form; list limited to /api/fx-convertible currencies, unknown saved values stay selectable). `src/components/PairPicker.tsx` is the pair dropdown for FORMS (flags + search like the Trading toolbar's PairSelect; fixed-positioned so modals can't clip it; `emptyLabel` for optional tags, `allowCustom` keeps free entry where imports need it) - used by risk, add-trade, analysis log, note and strategy pair tags. Don't reimplement these per-page.
- Hidden accounts (0020) are excluded from COMBINED views everywhere: dashboard all-accounts balance/net/curve (balance base = sum of visible account sizes when a ledger exists, else profile size), journal all-accounts, trading-day panel lifetime net. Unassigned trades always count; a hidden account's own numbers show only while it's selected. The risk calculator carries an AccountSwitcher - the selected account prefills size + currency (profile defaults on "All accounts").
- Accounts (prop-firm, migration 0019): `accounts` table (name, firm, phase challenge/verification/funded/personal/demo, size, currency, status active/passed/failed/closed, per-account guardrails, started/ended, successor_of links the journey) + `trades.account_id`. Managed at Settings > Accounts (`AccountsTab.tsx`: lifecycle buttons, Start successor prefills from the outcome, attach-unassigned-trades helper). Selection is client-side (`src/lib/accounts.ts`: localStorage `tb_account` + `tb:account-changed` event, `useSelectedAccount`, `effectiveGuardrails` - account limits win, user_settings are the defaults/fallback and keep routine/notify personal). `AccountSwitcher.tsx` renders nothing until accounts exist; used in Journal header, trading-day panel, and dashboard (`DashboardAccountBar` mirrors selection into `?account=` for the server page). Manual add + MT5 import carry an account picker; trade writes retry without `opened_at`/`account_id` until migrations land; ai-context lists the ledger with per-account nets (and retries its trades select without account_id).
- Dashboard (Aug 2026 facelift): account-scoped via `?account=`; hero band = balance + Net/Growth/Avg R chips beside the equity curve; per-account cards (click to scope, click again for all); stat tiles; the rest (daily bars, DOW, pairs, breakdown, kill zones) unchanged.
- Balance (derived, no broker): account size + lifetime net PnL. Shown as the first Dashboard stat and in the trading-day panel. The Aug 2026 trading-day panel additions: Balance row, compact window chips with per-window open dots, zero-anchored PnL bar, Today's news (top 5 of today from `/api/news` + link to /news), Watchlist chips (one-click symbol switch via `onPickSymbol`), today's trades.
- Trade durations: `trades.opened_at` (0018). MT5 import auto-maps the FIRST "Time" column as open (close stays the last); manual form has Open/Close time fields (UTC). Trade page shows Opened/Closed/Duration when both ends exist.
- Discipline score: Journal monthly summary tile; average of violation-free-day rate, journaled-trade rate, plan-followed verdicts (yes 1 / partial 0.5 / no 0), and routine completion on traded days; components without data are excluded, computed in JournalWorkspace.
- Weekly AI recap: week scope of JournalPanel has "Ask Sidekick" - POSTs a recap prompt to `/api/ai` (which already carries the full data snapshot) and streams the text into the panel.
- Tutorial: `TutorialTour.tsx` mounted in the (app) layout; 8-step spotlight tour over `data-tour` anchors on sidebar/mobile nav (auto-starts when user_settings.tutorial_done is false, replayable via the `tb:start-tour` window event from Settings; keyboard arrows/Enter/Escape).
- Profile: `/profile` display name, password change (current+new) in a separate Security section. Signup collects first/last name. Appearance, trading profile and pairs moved to Settings (Aug 2026).
- Layout: collapsible sidebar (state in localStorage), viewport-pinned; content scrolls inside. Pinch zoom enabled (no maximumScale). MobileNav is `fixed bottom-0` (NOT in-flow: iOS Safari toolbar collapse and the standalone --app-height lag made an in-flow bar float with a gap); main carries matching mobile bottom padding. The --app-height boot script skips updates while pinch-zoomed (visualViewport.scale guard).
- Database: schema + RLS in `supabase/migrations/` (0001 init, 0002 import ext_id, 0003 notes, 0004 traded_on -> timestamptz + commission, 0005 chart_analyses, 0006 pair tags on notes/strategies (strategy pair UI later removed; column kept), 0007 news_items headline archive, 0008 chart_analyses.extra_images, 0009 ai chat history, 0010 strategies.strategy_date + trading_window_2, 0011 ai_conversations pinned/unread/archived + drops its updated_at auto-trigger, 0012 ai_messages.image_path). Apply 0004 before MT5 import, 0006 before pair tags, 0007 before the news archive fills, 0010 before deploying the July 2026 strategy/Sidekick changes (both select the new columns). 0011 is optional at deploy time: the chat list falls back to the 0009 columns until it's applied, but the ⋮ menu flags won't persist. 0012 is optional too: Sidekick chart screenshots persist and reload once applied; before it, the message insert retries without image_path and images stay session-only. 0013 changes strategies.max_daily_loss/max_daily_profit/risk_per_trade_pct to text so they hold a figure ("200") or a percent of account ("5%") verbatim; apply it before deploying the figure-or-% risk controls (the save writes strings and ai-context resolves percents via account size). Aug 2026: 0014 trade_reviews (per-trade journal: plan_followed, strategy snapshot, tag arrays, emotions, reflection, htf/mtf/ltf image paths, unique per trade), 0015 day_reviews (per-day: plan_followed yes/partial/no, note, routine_done; violations are computed live, not stored), 0016 user_settings (guardrails, routine items jsonb, notification prefs, tutorial_done), 0017 trade_shares (public share links; owner-only RLS, resolved by the public page with the service-role key), 0018 trades.opened_at (durations; writes retry without the column until applied). All are safe to deploy code-first: every consumer detects the missing table/column and falls back with a notice, but the feature only persists once applied. 0021 backtesting (backtest_sessions, backtest_trades, shared candles cache) - same code-first rule.
- Pairs: master catalog in `src/lib/pairs.ts` (FX, metals, crypto, indices, energy); the user watchlist lives in auth metadata `pairs`, managed at Settings -> Trading pairs (`/settings?tab=pairs`; old `/profile/pairs` redirects), read via `usePairs()`. It drives every pair dropdown (charts, risk, journal, analysis log, note tags, strategy pair tag - restored July 2026). Add new instruments to the catalog only; sizing conventions live in `src/lib/risk.ts` (XAG 0.01/5000oz, crypto pip 1 contract 1 = coins; indices/energy not sizable). `/api/fx` covers fiat (Frankfurter), metals (gold-api), crypto (CoinGecko id map).
- Security: password minimum is 10 in the client; ALSO raise it in Supabase dashboard (Auth -> Passwords, still default 6) and enable leaked-password protection.
- Accessibility: form labels and primary buttons pass WCAG AA (see brand section); avoid reintroducing `#757C8E` text or `maximumScale: 1`. Light theme (`html[data-theme="light"]` in `globals.css`) is also AA-tuned: dim `#646B7E`, success `#087A52`, danger `#C92F35`, gold `#9A650F`; page bg darker than cards for elevation. Recheck contrast before changing any of these.
- Shared form input CSS (`.field`/`.input`/`.jfield`) lives in `globals.css`, not per-component style tags.
- Sidekick (AI assistant): `/sidekick` chat over the user's own data via Gemini (`gemini-3.1-flash-lite`, free-tier key in `GEMINI_API_KEY`, server-side only). `/api/ai` streams plain text; per request it rebuilds a compact context (`src/lib/ai-context.ts`: aggregates, breach days, strategies with criteria/risk controls, last 50 trades, last 10 notes full text (capped 1.2k chars), last 10 chart-analysis entries, last 15 archived news headlines) with the signed-in user's Supabase client so RLS applies. Rules (loosened July 2026): Sidekick MAY give advice, analyse charts and explain news when asked - opinions with reasoning, never certainty or guaranteed outcomes; claims about the trader's own performance must quote the data snapshot. Screenshot + selected strategy still gets a rule-by-rule check plus an overall opinion. Page awareness: the client sends the current pathname (`page` in the POST body) and the route maps it to a description (PAGE_NOTES) so "explain this page / this news / this analysis" resolves correctly. On Gemini 429 the route falls back to a free OpenRouter model (text only, `OPENROUTER_API_KEY`, live free-model list cached 1h, prefers Qwen/GLM/Kimi). Chat history persists in `ai_conversations`/`ai_messages` (migration 0009, RLS). Chart screenshots are now stored: on send the image is uploaded to the entry-models bucket and its path saved in `ai_messages.image_path` (migration 0012), re-signed on load so it renders when a chat is reopened; `has_image` stays as a fallback flag and the base64 still only rides to the model on the newest message. conversation list panel with New chat + a Claude-style ⋮ menu per chat (Pin, Mark as unread, Rename inline, Archive, two-step Delete; P/U/R/A/D shortcuts while open; pinned sort first, archived behind a footer toggle; columns from migration 0011, which also drops the updated_at auto-trigger so flag edits don't reorder the list). Mobile/compact keeps the plain history dropdown (pinned first, 📌 prefix, archived hidden). Slash commands in the composer: /note, /strategy, /analysis attach that item's content to the message (title stored as a `[kind: title]` prefix in ai_messages.content; /strategy fetches the written rules client-side on pick). Every open starts on a NEW chat (no auto-load of the latest conversation); history stays one click away. Dock (`SidekickDock` in the app layout, Aug 2026 redesign): a sparkle pill at the bottom, horizontally centred on the CONTENT column (absolute inside the app layout's relative wrapper, so it re-centres live as the sidebar is dragged/collapsed; expands into the ask bar on hover/focus; Cmd/Ctrl+I focuses it); submitting opens the right-hand panel with the question auto-sent (`pendingText`/`onPendingTextUsed` props on SidekickChat, mirroring pendingImage), clicking empty just opens the panel. The old draggable bubble is gone; mobile uses the Sidekick nav tab. The panel pushes a history entry so the phone's back gesture closes it, has a mobile Back button, and the bar hides while a strategy/note is in edit mode (`body[data-editing="1"] .sk-dock-launcher` rule in globals.css, flag set by StrategyWorkspace/NotebookWorkspace; `body[data-fab="1"]` lifts it above the floating Save pill). Concept mock: `design/tradebook-assistant-concept.html`.
- Strategies: optional `strategy_date` (date field, shown next to plan type in view mode) and `trading_window_2` (second session window, same "HH:MM-HH:MM TZ" text format) since migration 0010; both edited on /strategy and included in the AI context. Pair tag UI restored (column from 0006): select from the watchlist in edit mode, chip in view mode + list rows, in the AI context. The plan list has search (name/plan type/pair) and a date-range filter (uses strategy_date, falls back to created date), mirroring the Notebook list.
- Landing page (`src/app/page.tsx`): no "free"/pricing/self-hosted selling language anywhere (owner's rule - this is not being sold to anyone). Four feature cards (Plan/Trade/Review/Ask, Backtest folded into Plan) plus an "Also inside" line for the quieter features; live demo components in `src/components/landing/`: HeroCurve, RiskDemo, EntryChecklist, JournalDemo (mini month calendar), SidekickDemo (scripted AI chat), GuardrailsDemo (interactive PnL slider + trade dots + warnings). Sections scroll-reveal and demos lazy-mount via `Reveal.tsx` (IntersectionObserver, `.tb-reveal` in globals.css, reduced-motion safe), so nothing animates off-screen on load.
- Deploy: pushed to GitHub (`agimharizaj/tradebook`), auto-deploys to Vercel on push to `main`.
- Backtesting (manual replay, Aug 2026): `/backtest` (nav entry sits ABOVE Journal by owner's request). `BacktestWorkspace.tsx` = session list + new-session form (pair from watchlist limited to "/" labels, timeframes 1m-1w (the Twelve Data/Binance intersection, defined once in TIMEFRAMES in src/lib/backtest.ts - expand there, both providers must support any addition), UTC start date, optional plan, starting balance, risk %; AccountSwitcher prefills balance from the selected account, picking a plan pulls its risk_per_trade_pct, profile metadata is the fallback for both); `ReplayView.tsx` = lightweight-charts v5 candle replay (`ReplayChart.tsx`, 150 warmup bars, play/pause/step/+10, speeds 0.5x-10x, Space and ArrowRight/"." shortcuts) with Long/Short trade form (entry prefilled with bar close, stop required and side-validated, optional target), SL/TP auto-resolve on each bar high/low (stop counted FIRST if one bar sweeps both - conservative by design), manual close at bar close, live stats (win rate, avg R, expectancy, total R, PnL, inline equity curve) and the selected plan's entry_criteria as a tickable checklist (ticks reset on trade placement). Engine is pure functions in `src/lib/backtest.ts` (rMultiple, resolveTradeOnBar, computeStats; PnL = FLAT risk of starting balance per trade, not compounding - stated in the UI). Data via `/api/candles` (Binance keyless for crypto, Twelve Data for FX/metals - free key in `TWELVE_DATA_API_KEY`, server-side only, 800 req/day, max 5000 bars per request; route caps the window accordingly). Candles cache in `public.candles` (shared market data, deliberately not uid-scoped, authenticated-only RLS) so repeat replays cost no quota. Migration 0021 (backtest_sessions, backtest_trades, candles); results NEVER touch `trades`, so live expectancy/discipline/dashboard stats stay clean. Degrades gracefully pre-migration (in-memory replay + notice) and without the key (crypto still works). Sessions resume from `replayed_to`. Trades show open/close bar times + duration (bar-open granularity) and lot size + money PnL via `sizeFromRisk` with conversion 1 (assumes a quote-currency account, stated in the UI). Speeds go to 20x (50ms ticks, the browser timer ceiling) and 50x (5 bars per 100ms tick; stops still resolved bar-by-bar). A practice run (save unchecked) stores nothing, but "Save session" in the replay header promotes it mid-run: inserts the session row + all closed trades, then keeps persisting (`canSaveLater` prop, false when migration 0021 is missing; the exit button reads "Exit" not "Save & exit" while unsaved). ReplayView sets `body[data-replay="1"]` to hide the Sidekick dock bar (rule in globals.css, same mechanism as data-editing). Automated backtesting deliberately deferred: strategies are free-text, a rules DSL would be needed first.
- Industry benchmark: `industry-standards-review.md` (July 2026). Deferred from its list: StrategyWorkspace split, time-of-day reports, service worker.
- EdgeFlo parity concept: `design/tradebook-journal-concept.html` (Aug 2026, incl. gap analysis). Deferred from it: account balance/equity tracking, persistent charts watchlist panel, chart-snapshot-to-journal, "This month" fullscreen toggle; broker-sync items (live positions, in-chart execution, entry-emotion popup, Auto badge, split orders) stay on the MetaApi "later" roadmap. Share Trade public links deliberately skipped (single-user app).

## Phase roadmap (build in order, ship each before the next)

1. Auth — DONE.
2. Strategy builder — multiple strategies, each with: name, plan type, numbered charting process (add/remove/reorder), entry criteria (checkboxes, add/remove/reorder), entry-model screenshots (upload/remove via the `entry-models` storage bucket), trade management rules (add/remove/reorder), exit criteria (checkboxes, add/remove/reorder), free-text notes, and the risk-controls panel.
3. Journal — monthly calendar with per-day trade summaries you can open, plus weekly and monthly breakdowns.
4. Risk calculator — DONE.

Done beyond the core four: PWA (installable, manifest + icons), responsive mobile layout with bottom tab nav, Trading (TradingView embed with drawing tools + trading-day panel) at `/trading` (renamed from Charts, Aug 2026; `/charts` redirects with its query string), Notebook (notes CRUD, autosave) at `/notebook` (table in `0003_notes.sql`), News (TradingView market-news timeline + economic calendar + Market sessions section: live Sydney/Tokyo/London/New York clocks with open-closed status, overlap timeline and cross-market time converter, pure client-side Intl, no libs; session hours are indicative constants in `src/components/sessions/MarketClocks.tsx`) at `/news` (tabbed, Aug 2026: ticker tape persists on top, then News & calendar / Market sessions / Heatmap tabs, deep-linkable via `?tab=` read once on mount like Settings; TradingView widgets mount per tab on purpose - they missize inside display:none. The standalone `/sessions` page and its nav entry were folded into News; `/sessions` redirects to `/news?tab=sessions`, Sidekick PAGE_NOTES updated, and the trading-day panel gained a Market sessions strip - `SessionStrip.tsx`, chips with live open/closed dots + a "Clocks + converter" link - so sessions stay one tap from the chart on desktop and in the mobile bottom sheet), Sanctuary (box-breathing + psychology prompts) at `/sanctuary`. Profile moved to the sidebar footer.

Done beyond that: Sidekick, the AI assistant, at `/sidekick` (see Current status).

Later, not now: live broker sync (MetaApi), placing trades from the chart, richer note formatting.

## Definition of done per phase

`npm run build` and `npm run typecheck` pass; works end to end against my real Supabase project; deployed to Vercel and confirmed live; no RLS gaps (I only ever see my own rows).

## First-run commands

```bash
cp .env.local.example .env.local   # then fill in Supabase URL + anon key
npm install
npm run dev                         # http://localhost:3000
```

Full setup and deploy steps are in `README.md`. Paste `claude-project-instructions.md` into a claude.ai Project if you want to build there too.
